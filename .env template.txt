# ============================
# OBRIGATÓRIO
# ============================

# Postgres via SQLAlchemy (psycopg2). Formato:
# postgresql+psycopg2://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO[?parametros]
DATABASE_URL=postgresql+psycopg2://master:root@localhost:5432/mirai_session

# Redis via URL única. Formatos aceitos:
# - redis://[:SENHA]@HOST:PORTA/DB
# - rediss://[:SENHA]@HOST:PORTA/DB   (TLS)
REDIS_URL=redis://:minha_senha@localhost:6379/0

# ============================
# OPCIONAIS (se você usa)
# ============================

# Ex.: chave do LLM
GEMINI_API_KEY=coloca_sua_chave_aqui

# Ambiente / logs
APP_ENV=dev
LOG_LEVEL=INFO

# Se seu Postgres gerenciado exige SSL:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@host:5432/db?sslmode=require

# Se estiver em Docker Compose, use o nome do serviço como host:
# DATABASE_URL=postgresql+psycopg2://master:root@postgres:5432/mirai_session
# REDIS_URL=redis://:minha_senha@redis:6379/0

# ============================
# REQUIRED
# ============================

# Postgres via SQLAlchemy (psycopg2). Format:
# postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DB_NAME[?params]
DATABASE_URL=postgresql+psycopg2://master:root@localhost:5432/mirai_session

# Redis via single URL. Accepted formats:
# - redis://[:PASSWORD]@HOST:PORT/DB
# - rediss://[:PASSWORD]@HOST:PORT/DB   (TLS)
REDIS_URL=redis://:minha_senha@localhost:6379/0

# ============================
# OPTIONAL (if you use)
# ============================

# Example: LLM key
GEMINI_API_KEY=put_your_key_here

# Environment / logs
APP_ENV=dev
LOG_LEVEL=INFO

# If your managed Postgres requires SSL:
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/db?sslmode=require

# If running on Docker Compose, use the service name as host:
# DATABASE_URL=postgresql+psycopg2://master:root@postgres:5432/mirai_session
# REDIS_URL=redis://:minha_senha@redis:6379/0
